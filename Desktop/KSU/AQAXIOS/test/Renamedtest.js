/*const axios = require ('axios');

test ('GET/posts returns post', async() => {
    const response = await axios.get(''/posts);

    expect(response.status).toBe(200);
    expect(response.data.id).toBe (1);
} ); */